<Application
    x:Class="ConfGTS.Client.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <controls:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <Color x:Key="BrandNavy">#092B56</Color>
            <Color x:Key="BrandBlue">#157EAD</Color>
            <Color x:Key="BrandCyan">#14B7BC</Color>
            <SolidColorBrush x:Key="BrandNavyBrush" Color="{StaticResource BrandNavy}"/>
            <SolidColorBrush x:Key="BrandBlueBrush" Color="{StaticResource BrandBlue}"/>
            <SolidColorBrush x:Key="BrandCyanBrush" Color="{StaticResource BrandCyan}"/>
            <SolidColorBrush x:Key="MutedBrush" Color="#65748C"/>
            <SolidColorBrush x:Key="LineBrush" Color="#D9E4F1"/>

            <Style TargetType="Button" x:Key="PrimaryButtonStyle">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource BrandBlueBrush}"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="18,12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="CornerRadius" Value="11"/>
                <Setter Property="Padding" Value="12,9"/>
            </Style>
            <Style TargetType="PasswordBox">
                <Setter Property="CornerRadius" Value="11"/>
                <Setter Property="Padding" Value="12,9"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
